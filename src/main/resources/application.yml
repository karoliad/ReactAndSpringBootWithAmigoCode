spring:
  profiles:
    active: @spring.profiles.active@

app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/karolinedemo
    username: karoline_admin
    password: password
    pool-size: 30
